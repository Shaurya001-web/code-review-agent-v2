Core parsing module for the code review agent.Core parsing module for the code review agent. Recursively walks the Python AST using tree-sitter to extract function-level chunks with name, source code, and line boundaries — the unit every downstream review node operates on.
